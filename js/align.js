/* ============================================================
   ditto — 시대 간 위치 대응표

   규약:  리메이크_시각 = 원곡_시각 + offset   (초)

   왜 필요한가:
     iTunes 미리듣기는 30초 '발췌' 인데, 애플이 원곡과 리메이크에서 각각 다른
     지점을 잘라낸다. 그래서 두 발췌의 '같은 12.3초' 는 음악적으로 무관하고,
     셔틀로 넘어가면 가사가 되돌아가거나 엉뚱한 데로 튄다.
     (길이 차이는 원인이 아니다 — 20개 전부 29.98~30.00초로 실측됐다.)
     발췌 시작점은 iTunes API 가 알려주지 않아 계산으로는 못 구한다.

   지금 비어 있는 이유 — 자동 정렬을 두 방식으로 시도했고 둘 다 같은 벽에 부딪혔다:

     1) 크로마(화성) 기반 — tools/align.html
        정직한 제약(겹침 60% 이상, 조옮김 ±2반음)을 걸자 신뢰도가 0.06~0.47 로
        무너졌다. 임계값 0.45 를 넘는 건 열 쌍 중 하나뿐.
        제약을 풀면 숫자는 좋아 보이지만 30초 클립에 ±24초 오프셋(겹침 5초) 같은
        값이 나온다. 그건 정렬이 아니라 우연히 닮은 한 조각이다.

     2) 온셋(비트·음의 시작점) 기반 — tools/estimate_offsets.py
        화성이 안 되면 리듬은 될까 싶어 특징을 바꿔봤다. 10쌍 전부 실측한 결과가
        r = 0.082~0.470, 0.45 를 넘은 건 역시 한 쌍(pair-01)뿐이고 그마저
        탐색 경계(-8.15초)에 붙어 있어 신뢰할 수 없다. 1) 과 사실상 같은 결과다.

     원곡과 리메이크는 템포·조성·편곡·보컬이 모두 달라서, 30초 발췌만으로는
     무엇을 재든 충분히 겹치지 않는다. 특징의 문제가 아니라 데이터의 문제다.
     틀린 보정은 보정을 안 하느니만 못해서 넣지 않았다.

   채우는 방법 (귀로 하는 쪽이 확실하다):
     같은 가사·같은 후렴 첫 마디가 각 발췌에서 몇 초에 나오는지 재고,
       offset = (리메이크에서의 초) - (원곡에서의 초)
     예) 후렴이 원곡 발췌 12.4초, 리메이크 발췌 5.8초 → offset: -6.6
     confidence 는 직접 채운 값이면 1 로 둔다. 0.45 미만이면 앱이 무시한다.

   한 쌍씩 채워도 된다. 없는 쌍은 지금처럼 같은 초로 넘어간다.

   귀로 잴 때 어디부터 들어볼지 힌트 (자동 추정치 — 데이터가 아니다):
     pair-01 -8.15 (r .47)   pair-02 -2.53 (r .17)   pair-03 -1.51 (r .12)
     pair-04 -2.79 (r .16)   pair-05 -0.51 (r .21)   pair-06 +7.94 (r .14)
     pair-07 +3.72 (r .23)   pair-08 +0.28 (r .19)   pair-09 -9.08 (r .08)
     pair-10 -3.78 (r .12)
   r 이 전부 0.45 언저리이거나 그 아래다. 맞을 수도 있고 아닐 수도 있다는 뜻이니
   시작점으로만 쓰고, 실제로 들어서 확인한 숫자만 아래에 넣을 것.
   ============================================================ */

window.DITTO_ALIGN = {
  // 'pair-01': { offset: -6.6, confidence: 1 },
};
